<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Botón general -->
    <style name="AppButton" parent="Widget.MaterialComponents.Button">
        <item name="backgroundTint">@color/primaryColor</item>
        <item name="android:textColor">@android:color/white</item>
        <item name="cornerRadius">16dp</item>
        <item name="android:fontFamily">@font/poppins</item>
    </style>

    <!-- Título grande -->
    <style name="TitleText" parent="TextAppearance.MaterialComponents.Headline5">
        <item name="android:textColor">@color/textColor</item>
        <item name="android:fontFamily">@font/poppins</item>
    </style>

    <!-- Subtítulo o descripción -->
    <style name="BodyText" parent="TextAppearance.MaterialComponents.Body1">
        <item name="android:textColor">@color/textColor</item>
        <item name="android:fontFamily">@font/poppins</item>
    </style>

    <!-- Estilo para ítems del RecyclerView -->
    <style name="PaletteItemStyle">
        <item name="android:background">@drawable/palette_item_background</item>
        <item name="android:padding">12dp</item>
        <item name="android:layout_marginBottom">8dp</item>
    </style>
</resources>
